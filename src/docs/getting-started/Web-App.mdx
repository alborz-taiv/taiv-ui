import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Getting Started/Note: About Partner Web App" />

# **A Note About Partner Web App**

When sizing in the Partner App specifically, please prefer `px` over `rem` for values like gaps, paddings, margins, font sizes, etc. (contradictory to usual Frontend Standards).
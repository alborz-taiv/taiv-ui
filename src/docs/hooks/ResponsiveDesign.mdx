import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Hooks/Responsive Design" />

# Responsive Design

Media queries are used to create responsive designs. They are based on the width of the viewport.

### 📦 Import

```tsx
import { useMobile, useTablet, useLaptop, useDesktop, useWide } from '@taiv/ui/hooks';
```

### 📐 Basic Usage

```tsx
const isMobile = useMobile();

if (isMobile) {
  return <Button size="sm" fullWidth> Mobile Optimized </Button>;
}
else {
    return <Button size="lg"> Default UI </Button>;
}
```

### 📐 Breakpoint Values

<table>
  <thead>
    <tr>
      <th>Breakpoint</th>
      <th>Width</th>
      <th>Hook</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Mobile</td>
      <td>640px</td>
      <td>useMobile()</td>
    </tr>
    <tr>
      <td>Tablet</td>
      <td>768px</td>
      <td>useTablet()</td>
    </tr>
    <tr>
      <td>Laptop</td>
      <td>1024px</td>
      <td>useLaptop()</td>
    </tr>
    <tr>
      <td>Desktop</td>
      <td>1280px</td>
      <td>useDesktop()</td>
    </tr>
    <tr>
      <td>Wide</td>
      <td>1536px</td>
      <td>useWide()</td>
    </tr>
  </tbody>
</table>